# URL of the backend API (no trailing slash)
# Example for local dev: http://localhost:3001
# Example for production: https://api.yourdomain.com
# REQUIRED — the app will throw an error on startup if this is missing.
PUBLIC_API_URL=http://localhost:3001
